@using EducationCode.Models;
@model List<CHUDEBLOG>

    <li class="nav-item">
        <div class="dropdown">
            <a class="dropdown-toggle nav-link" id="dropdownMenu2" data-toggle="dropdown" href="#">
                Blog
            </a>
            <div class="dropdown-menu" aria-labelledby="dropdownMenu2">
                @foreach (var item in Model)
                {
                    <a class="dropdown-item" href="@Url.Action("LocChuDeBlog","ChuDeBlog", new {machude = item.MACHUDEBLOG })">@item.TENCHUDE</a>
                }
            </div>
        </div>
    </li>