@using EducationCode.Models;
@model List<KHOAHOC>
@{
    Layout = null;
}

    <li class="nav-item">
        <div class="dropdown">
            <a class="dropdown-toggle nav-link" id="dropdownMenu2" data-toggle="dropdown" href="#">
                Khóa học
            </a>
            <div class="dropdown-menu" aria-labelledby="dropdownMenu2">
                @foreach(var item in Model)
                    {
                    <a class="dropdown-item" href="@Url.Action("ChiTietBaiVietMN","ChiTietKhoaHoc", new {makhoahoc = item.MAKHOAHOC})">@item.TENKHOAHOC</a>
                }
            </div>
        </div>
    </li>